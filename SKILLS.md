# Skills available

- `add-api-service-method`: SOP for adding a new API endpoint (handler + route + repository)
- `code-review`: Review the current diff for correctness bugs
- `security-review`: Security review of pending changes on the current branch
- `verify`: Build and run the server locally to confirm a change works
